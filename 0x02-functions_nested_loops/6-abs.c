#include "main.h"

/**
 * _abs - function to print the absolute value of a number
 *
 * @n: number to determine absolute value
 *
 *return: 0
 */

int _abs(int)
{
	int c;

	if (c >= 0)
		return (c);
	else
		return (c * -1);
}
