#include "main.h"
#include <stdio.h>

/**
 * _puts - function to print a string followed by a newline to stdout
 * @str: string variable to be printed
 * Return: 0
 */

void _puts(char *str)
{
	puts(str);

}
